workflows:
  default-workflow:
    name: HWO Flutter Workflow
    max_build_duration: 30
    environment:
      flutter: stable
    scripts:
      - name: Clean project cache
        script: flutter clean
      - name: Get dependencies
        script: flutter pub get
      - name: Build APK
        script: flutter build apk --release
    artifacts:
      - build/**/outputs/**/*.apk
